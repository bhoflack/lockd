# Automatically added by dh_installinit
if [ -x "/etc/init.d/lockd" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lockd stop || exit $?
	else
		/etc/init.d/lockd stop || exit $?
	fi
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/lockd" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lockd stop || exit $?
	else
		/etc/init.d/lockd stop || exit $?
	fi
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/lockd" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lockd stop || exit $?
	else
		/etc/init.d/lockd stop || exit $?
	fi
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/lockd" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lockd stop || exit $?
	else
		/etc/init.d/lockd stop || exit $?
	fi
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/lockd" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lockd stop || exit $?
	else
		/etc/init.d/lockd stop || exit $?
	fi
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/lockd" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lockd stop || exit $?
	else
		/etc/init.d/lockd stop || exit $?
	fi
fi
# End automatically added section
